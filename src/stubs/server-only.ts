// No-op stub for Next.js's `server-only` marker.
// The standalone backend is entirely server-side; nothing can leak into a
// client bundle, so the "this is server-only" guard is meaningless here.
export {};
